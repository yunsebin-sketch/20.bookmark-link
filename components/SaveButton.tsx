export default function SaveButton() {
  return (
    <button
      type="submit"
      className="hover-accent flex items-center justify-center rounded-md bg-[var(--accent)] px-5 py-2 text-sm font-medium text-white"
    >
      저장
    </button>
  );
}
